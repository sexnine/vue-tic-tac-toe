import { library } from "@fortawesome/fontawesome-svg-core";
import { faComputer, faX, faO } from "@fortawesome/free-solid-svg-icons";

// @ts-ignore  idk why typescipt is being annoying over this
library.add(faComputer, faX, faO);

export default library;
